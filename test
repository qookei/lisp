(1 . 2) 3

(define my-number 7)

(define (list kar . rest)
    (cons kar rest))

(define (faux-macro a)
    (list a 10 20))

(define (fact n)
    (if (eq? n 0)
	1
	(* n (fact (+ n -1)))))

(fact my-number)

(eval (faux-macro '+))
